@model List<SimpleSocialNetwork.WebUI.PostServiceReference.PostDto>

<ul id="posts">
    @if (Model != null)
    {
        foreach (var post in Model.OrderByDescending(x => x.DatePosted))
        {
            Html.RenderPartial("PostPartial", post);
        }
    }
</ul>
