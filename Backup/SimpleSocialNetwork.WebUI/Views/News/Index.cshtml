@model List<SimpleSocialNetwork.Domain.Post>

@{
    ViewBag.Title = "News";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!--


--><div id="content">
     <h2>News</h2>
    <ul id="posts">
    @if (Model != null)
    {
        foreach (var post in Model)
        {
            Html.RenderPartial("PostPartial", post);
        }
    }
    </ul>
</div>
<script src="~/Scripts/postAndCommentsScript.js"></script>